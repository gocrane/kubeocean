{{- if .Values.manager.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kubeocean.manager.clusterRoleBindingName" . }}
  labels:
    {{- include "kubeocean.manager.labels" . | nindent 4 }}
subjects:
- kind: ServiceAccount
  name: {{ include "kubeocean.manager.serviceAccountName" . }}
  namespace: {{ .Values.namespace.name }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kubeocean.manager.clusterRoleName" . }}
{{- end }}
