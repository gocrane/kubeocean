apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{.DeploymentName}}"
  namespace: "{{.SyncerNamespace}}"
  labels:
    app: kubeocean-syncer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubeocean-syncer
  template:
    metadata:
      labels:
        app: kubeocean-syncer
    spec:
      serviceAccountName: "{{.ServiceAccountName}}"
      containers:
      - name: syncer
        image: kubeocean-syncer:latest
        args: ["--cluster-binding-name", "{{.ClusterBindingName}}"]

