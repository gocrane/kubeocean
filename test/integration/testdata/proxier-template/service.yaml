apiVersion: v1
kind: Service
metadata:
  name: "{{.ServiceName}}"
  namespace: "{{.ProxierNamespace}}"
  labels:
    app: kubeocean-proxier
spec:
  selector:
    app: kubeocean-proxier
  ports:
  - port: 8080
    targetPort: 8080
    name: http
